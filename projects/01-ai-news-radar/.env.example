# Optional. If absent, the project uses a local extractive summarizer.
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4o-mini

# Optional. Limits how many items are fetched per source.
ITEMS_PER_SOURCE=8
