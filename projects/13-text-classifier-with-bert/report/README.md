# Report

Generated files:

- `model.json`: class prototypes learned from the tiny training split.
- `metrics.json`: evaluation results on the tiny test split.

Run:

```bash
npm run verify
```

The report is intentionally small and local so the project stays runnable without GPU, network access, or external model downloads.
